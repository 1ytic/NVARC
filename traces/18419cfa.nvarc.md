<rules_summary>
Extend the 2s horizontally in both directions until they encounter 8s.
</rules_summary>
<input_generation>
Input grids are provided as examples. The transformation rule must be inferred from the training examples.
</input_generation>
<solution_steps>
1. Locate all occurrences of 2 in the input grid.
2. For each 2, extend it horizontally to the left until an 8 is encountered or the edge of the grid is reached.
3. For each 2, extend it horizontally to the right until an 8 is encountered or the edge of the grid is reached.
</solution_steps>
<key_insight>
The input grid contains blocks of 8s surrounding areas where a 2 is present.
</key_insight>
<puzzle_concepts>
- color_mapping
- connected
- mirroring
</puzzle_concepts>
